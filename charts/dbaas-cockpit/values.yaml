# Default values for dbaas-cockpit.
replicaCount: 1
image:
  repository: nginx
  pullPolicy: IfNotPresent
  pullSecrets: []
  # tag: "1.25"
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
service:
  type: ClusterIP
